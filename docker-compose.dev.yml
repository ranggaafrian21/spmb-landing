# Development override - use this locally to build the image from source
# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build
services:
  spmb-landing:
    build:
      context: .
      dockerfile: Dockerfile
